{
  "name": "webhub-crm",
  "version": "1.0.0",
  "description": "Web Hub CRM - Personal freelancer CRM for Facebook outreach and client tracking",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "start": "node server/server.js",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "seed": "node server/src/seed/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
